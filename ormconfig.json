{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "postgres",
  "logging": true,
  "entities": ["src/modules/**/entities/*.ts"],
  "migrations": ["src/shared/providers/typeorm/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/shared/providers/typeorm/migrations"
  },
  "synchronize": false
}
